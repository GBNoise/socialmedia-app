import React from "react";
import styles from "../../styles/lives.module.scss";

export const LivesHeader = () => {
  return (
    <section className={styles.livesHeader}>
      <div className={styles.mainLive}></div>
    </section>
  );
};
